// Single source of truth for org-specific branding.
// To rebrand the app, change ORG_NAME here — every label, logo, and placeholder
// that references the org name derives from this constant.
export const ORG_NAME = 'MGI';
