{
  "name": "psr-communication-tool",
  "version": "1.0.0",
  "private": true,
  "description": "MGI centralized PSR communication portal for Project Managers",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --include=dev --prefix client",
    "dev": "concurrently -n SERVER,CLIENT -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "seed": "npm run seed --prefix server",
    "build": "npm run install:all && npm run build --prefix client",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
