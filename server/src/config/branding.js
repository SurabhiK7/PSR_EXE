'use strict';

// Single source of truth for org-specific branding on the server side.
// To rebrand, change ORG_NAME/TAGLINE here — PR-ID prefixes, contact groups, default settings,
// and generated report/email text all derive from these constants.
const ORG_NAME = 'MGI';
const TAGLINE = 'Unstoppable Professional Services Delivery';

module.exports = { ORG_NAME, TAGLINE };
